# Voice venv pins - bumped 2026-08-30 against PyPI (1.7.0 -> 1.8.1: the
# proposed-turn-frame rework; grammar_gate and session_runtime carry the
# compensations). Pipecat moves weekly: re-read its docs for every service
# used and re-run tests/ before bumping. The chord listener stays on system
# python.
pipecat-ai[local,silero,deepgram,anthropic,openai,tracing]==1.8.1
# Sentry's OTLP endpoint is HTTP-only (no gRPC), and pipecat's own tracing
# example imports the gRPC exporter - pin http so it cannot resolve by accident.
opentelemetry-exporter-otlp-proto-http==1.44.0
# Errors as Issues, plus OTLPIntegration's event linking (an exception lands on
# the span it broke). NO [opentelemetry-otlp] extra: that pulls
# opentelemetry-distro, which resolves its own otel versions against the pin
# above. The integration's imports are already satisfied by that exporter and
# opentelemetry-sdk - if a future version needs the extra, it raises
# DidNotEnable and telemetry/sentry.py logs lane_disabled rather than crashing.
sentry-sdk==2.68.1
openwakeword==0.6.0
hassil==3.12.0
rapidfuzz==3.14.6
# dispatch.py reaches tv.exlink_send_hex, which imports serial lazily, so the
# venv needs this as well as system python (k15\system-requirements.txt).
pyserial==3.5
# grammar_gate.py imports yaml at MODULE scope to read grammar.yaml. It arrives
# as a pipecat transitive; named here so a pipecat that drops it fails at
# install rather than at the first wake word.
PyYAML==6.0.3
# steam_session.py imports it lazily, as do library.py and steamstore.py in the
# chord lane. Also a pipecat transitive today; same reason to name it.
requests==2.34.2
pyflakes==3.4.0   # test-only: undefined-name guard (test_lint.py)
# how-long-to-beat hours (assistant game-details facet). library.py imports it
# lazily and fail-soft. New transitives not yet in constraints.txt:
# fake_useragent, beautifulsoup4, aiounittest.
howlongtobeatpy==1.0.23
# TV remote keys over WebSocket (tv_remote.py) - the volume write path that
# works with eARC audio; imported lazily and fail-soft. New transitives not
# yet in constraints.txt: websocket-client, yarl.
samsungtvws==3.0.5
# Transitives are enforced via constraints.txt (the supervisor installs with
# `-c constraints.txt`); its header has the regeneration rule. Clean resolve
# 2026-08-30 (cp311 dev box): pyaudio 0.2.14 (cp313 win_amd64 wheel exists),
# onnxruntime 1.24.4 (pipecat caps ~=1.24.3), deepgram-sdk 7.8.0,
# anthropic 0.125.0 (pipecat caps <1), openai 2.54.0 (pipecat caps <3),
# numpy 2.4.6 (numba caps <2.5),
# opentelemetry-{api,sdk,proto} 1.44.0 (+ semantic-conventions 0.65b0).
