# Voice venv constraints - as-built transitive versions, enforced at install
# (Start-Voice.bat passes `-c constraints.txt`).
#
# Regenerate ON THE K15 after a deliberate upgrade. Let cmd own the
# redirection - PowerShell 5.1's `>` writes UTF-16, which is not a pins file:
#   cmd /c ".venv\Scripts\python -m pip freeze > constraints.txt"
# Touch requirements.txt too when the new pins are meant to INSTALL: the
# supervisor's gate compares THAT file alone against .venv\deps-ok, so a
# constraints-only change would not install itself. A freeze that merely
# records a venv which already resolved needs no touch.
#
# Re-seeded 2026-08-30 from a clean cp311 resolve of the pipecat 1.8.1 pin
# set; a real K15 (cp313) freeze should add the deeper transitives - do NOT
# freeze from a cp311 box, its list would miss audioop-lts (py>=3.13 only).
# semantic-conventions 0.65b0 is sdk 1.44.0's lockstep partner under its own
# 0.x scheme.
pyaudio==0.2.14
onnxruntime==1.24.4
deepgram-sdk==7.8.0
anthropic==0.125.0
openai==2.54.0
numpy==2.4.6
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-proto==1.44.0
opentelemetry-semantic-conventions==0.65b0
