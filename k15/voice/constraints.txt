# Voice venv constraints - the AS-BUILT transitive versions, enforced at
# install time (Start-Voice.bat passes `-c constraints.txt`), so a rebuild
# months from now gets the bytes that were proven on this rig rather than a
# fresh resolve. That drift is not hypothetical: a clean resolve of the same
# requirements on 2026-08-12 already picked deepgram-sdk 7.7.0 and openai
# 2.54.0 over the as-built 7.6.0/2.53.0.
#
# Regenerate ON THE K15 after any deliberate upgrade:
#   .venv\Scripts\python -m pip freeze > constraints.txt
# then also touch requirements.txt's freeze-date line: the supervisor's
# reinstall gate compares requirements.txt ALONE, so a constraints change
# must ride a requirements change to install itself.
#
# Seeded 2026-08-12 from the as-built versions recorded in requirements.txt's
# comment (a full `pip freeze` from the live K15 venv should replace this
# seed - it will add the deeper transitives this list does not name).
# opentelemetry-semantic-conventions is 0.65b0, not 1.44.0: the semconv
# package versions in lockstep with sdk 1.44.0 under its own 0.x scheme.
pyaudio==0.2.14
onnxruntime==1.24.4
deepgram-sdk==7.6.0
anthropic==0.121.0
openai==2.53.0
numpy==2.4.6
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-proto==1.44.0
opentelemetry-semantic-conventions==0.65b0
