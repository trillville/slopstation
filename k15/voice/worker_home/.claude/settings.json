{
  "_comment": "ClaudeWorker guardrails. Since 2026-08-12 workers get NO Bash (workers.py --allowedTools): research-only is structural, and these deny rules are the secrets backstop behind it and AGENTS.md's ground rules - the injection canary drill (voice-testing 10c) is what proves all three on this machine. Codex's equivalent is its workspace-write sandbox (which keeps a shell - doctor warns on that lane). NOTE: Edit(...) covers every file-editing tool - a Write(...) deny is not evaluated by file permission checks and the CLI warns about it at startup.",
  "permissions": {
    "deny": [
      "Read(**/secrets.json)",
      "Read(**/config.ini)",
      "Edit(../**)"
    ]
  }
}
