name: slopstation-media

# Replace ``latest`` tags with the versions verified on the K15 before pulling.

services:
  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr:latest
    restart: unless-stopped
    environment:
      LOG_LEVEL: info
      TZ: ${TZ:-America/Los_Angeles}
    expose:
      - "8191"

  prowlarr:
    image: lscr.io/linuxserver/prowlarr:latest
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Los_Angeles}
    volumes:
      - type: bind
        source: ${MEDIA_CONFIG_ROOT}/prowlarr
        target: /config
    ports:
      # LAN access requires authentication and a private-profile firewall rule.
      - 9696:9696
    depends_on:
      - flaresolverr

  radarr:
    image: lscr.io/linuxserver/radarr:latest
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Los_Angeles}
    volumes:
      - type: bind
        source: ${MEDIA_CONFIG_ROOT}/radarr
        target: /config
      - type: bind
        source: ${MEDIA_ROOT}
        target: /data
    ports:
      - 7878:7878

  sonarr:
    image: lscr.io/linuxserver/sonarr:latest
    restart: unless-stopped
    environment:
      PUID: ${PUID:-1000}
      PGID: ${PGID:-1000}
      TZ: ${TZ:-America/Los_Angeles}
    volumes:
      - type: bind
        source: ${MEDIA_CONFIG_ROOT}/sonarr
        target: /config
      - type: bind
        source: ${MEDIA_ROOT}
        target: /data
    ports:
      - 8989:8989

  homarr:
    image: ghcr.io/homarr-labs/homarr:latest
    restart: unless-stopped
    environment:
      # 64 hex chars; Start-Media.ps1 generates it into .env. Changing it
      # orphans every credential Homarr has stored.
      SECRET_ENCRYPTION_KEY: ${SECRET_ENCRYPTION_KEY}
      TZ: ${TZ:-America/Los_Angeles}
    volumes:
      - type: bind
        source: ${MEDIA_CONFIG_ROOT}/homarr
        target: /appdata
      # Docker-containers widget. The socket grants container control, so
      # board actions stay auth-gated; anonymous viewers are read-only.
      - type: bind
        source: /var/run/docker.sock
        target: /var/run/docker.sock
    ports:
      # Host 8575 because VirtualHere owns 7575.
      - 8575:7575

  glances:
    # Feeds Homarr's system-resources and disk widgets. Internal-only:
    # Homarr polls http://glances:61208 server-side.
    image: nicolargo/glances:latest-full
    restart: unless-stopped
    environment:
      GLANCES_OPT: -C /etc/glances.conf -w
      TZ: ${TZ:-America/Los_Angeles}
    volumes:
      - type: bind
        source: ./glances.conf
        target: /etc/glances.conf
        read_only: true
      # These mounts let Glances report free space for both volumes.
      - type: bind
        source: ${MEDIA_ROOT}
        target: /mnt/media
        read_only: true
      - type: bind
        source: ${MEDIA_CONFIG_ROOT}
        target: /mnt/k15-system
        read_only: true
    expose:
      - "61208"
