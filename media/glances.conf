# Docker Desktop surfaces Windows bind mounts as 9p, which the fs plugin
# hides by default - without the allow, every drive gauge is empty.
# /etc/* are the VM-disk resolv.conf/hostname/hosts binds, pure noise.
[fs]
allow=9p
hide=/etc/.*
