# Voice build — assumptions & open questions ledger

Built blind on the `voice` branch (no live hardware/keys during the build, per
plan). Every judgment call that would normally be a paste-back lands here
instead. **Verdict column is yours** — fill it during the hardware pass; rows
with a failed verdict become the fix list.

> **Hardware pass, 2026-08-10 — gate passed, merged to main.** Proven live on
> the K15/TV/PC in one evening: wake → pre-roll ("hey jarvis play armored core
> six" as one sentence) → strip → grammar → fuzzy resolution → cold couch
> launch → AC6 actually starting (after the marker-ACL fix), plus validated
> Ex-Link sends, the switch-to-pc rework (row 3), the canned-echo verdict
> (row 4), and the BT self-heal (rows 10/19). Rows still open after merge are
> tuning-taste, not gate items: volume step feel (5), earcon tones (6), hold
> window feel (9), launch-refusal drills (13, partially), TTS voice audition
> (15), follow-up carry drill (17), and the provider A/B (18, needs
> `openaiApiKey`).

Legend: BT = blind-tested during the build (unit/file-driven/local-real) ·
HW = needs the hardware pass · KEY = needs a real key.

| # | Area | Assumption / decision | Why | Resolves by | Verdict |
|---|---|---|---|---|---|
| 1 | keys | Placeholder secrets: any value starting with `PLACEHOLDER` (or the template's `...` forms) disables that lane with a clear startup + doctor message, never a crash | Fail-soft rule | BT (unit) + your eyes at first run | |
| 2 | dispatch | `couch.py` is spawned under the **voice venv's** python (`sys.executable`), not system python — venv now carries pyserial so couch runs fine under it | One interpreter concept per process tree; listener precedent uses its own `sys.executable` | BT (spawn-args unit test) + HW: first voice-started session | |
| 3 | dispatch | "switch to the pc" = **get me gaming** (reworked 2026-08-10, user overruled the refusal UX): with no session it **starts one** — identical to "start a session", and more intuitive; mid-launch it answers a truthful "still starting" (busy earcon); with a READY session it flips instantly (live `ssh status`). Lock checked locally FIRST, so a sleeping PC costs no ssh timeout before the launch kicks off. The one rule is untouched: couch.py still switches the input only at READY — the old refusal-with-hint was enforcing the rule by lecturing when the system could just satisfy it | The rule was never "refuse" — it's "never show a dead input"; starting a session honors it while doing what the user meant | HW: input drill — say "switch to the pc" cold and watch a full session launch | reworked per user call |
| 4 | dispatch | **CLOSED: mute is a blind toggle, permanently.** The S90C acks the volume query and returns bytes — but the `decode_volume` drill (2026-08-10) proved the response is a **constant canned echo**, byte-identical at volume 7, volume 23, muted, and unmuted: `030cf1` (ack) + `030cf5` (status frame) + `08 f0010000 f1 16` (our own query bytes parroted back with a success code and checksum) + `0000fc`. The set acknowledges the query family without reporting values — there is no state to read, on this set, full stop. Blind toggle + vol_set-as-resync is the permanent design. Side win banked along the way: the ack bytes are proven live, so `exlink_send_hex` **validates every send** (`EXLINK_ACK`, raises `ExlinkNak` on NAK/silence — dispatch turns it into a fail earcon + abort-on-first-failed-ack; couch.py's power/input sends stay non-fatal by design) | A canned echo is stronger proof than silence: the query was received, processed, and still carries no data — modern Samsung sets dropped real status reporting | Done — `decode_volume` diffs all identical | blind toggle forever (decode drill 2026-08-10) |
| 5 | dispatch | "volume up" = `volumeStep` (5) single-step frames, 50 ms apart, abort on first failed ack | Matches remote-button feel; absolute set exists for jumps | HW: volume drill — tune step count by feel | |
| 6 | earcons | **Reworked 2026-08-11 (sound UX pass).** *Timing*: the wake chime is armed at detection, not played — it fires when you STOP talking, from whichever side hears it first (the `WakeCapture` watcher while the mic is still ours: RMS vs. the wake phrase's own level, 350 ms gap, forced at 1.5 s in a room too loud to call; else `GrammarGate` at end of turn). It therefore never lands over "hey jarvis put on Elden Ring", and it fills the wait before the answer instead of the silence before the command. *Sound*: all seven are now bells (light partials, exponential decay, peak-normalized) and tuned as one family — wake an ascending fifth G5→D6, close the same fifth descending, ok one bell on D6 ("yes, that note"), busy the same note struck twice (a knock at a shut door), fail three falling (F5→D5→A4), think one soft tap, announce a rising third. **Counts are still the contract** (1/2/3, mirroring the haptic thuds); pitch, contour and level are taste. Level order follows how often and how unasked each arrives — think 551 < close 655 < wake 933 < acks 1062–1413 < announce 1561 RMS, against the old flat tones' 4800–6000. `ok` fires on every command, so it has to read as an answer, not an alarm; `think` repeats every 3 s, so it is the quietest thing in the set; `announce` arrives across a dormant room, so it is the loudest. *Folding* (live 2026-08-11): a local command dispatches ~100 ms after the chime, so the two ran together — a plain `ok` inside `ACK_COALESCE_S` (0.8 s) of the chime is now swallowed (silence after the chime = it worked), while `busy`/`fail` always play, and a slow action (launch, ssh switch) clears the window and acks when it lands. Deleting `ok` outright was the other option and is wrong: it is the only signal a multi-second action finished. *Sleep*: every session end chimes, from the wake loop after teardown — exit phrase, idle and crash all sound the same (crash says `fail`). Volume is now a config knob, `voice.earconGain` | A chime over your own command is the jarring part; a decaying bell is perceptually far softer than a flat sine at the same peak; and "did it go back to sleep?" had no answer at all | HW: `--earcons` audition, then §5 rhythm drill — retune `earconGain`/specs freely, tests pin counts, lengths and that the bookends stay quieter than the acks | |

| 7 | architecture | **Per-session pipeline**: wake detection runs outside Pipecat (raw PyAudio + openWakeWord loop); each wake builds and runs a fresh `PipelineWorker` (mic → Flux → GrammarGate → speaker), torn down at session end | Pinned-source recon: Flux connects on `StartFrame` with no app-facing connect/disconnect, and its watchdog injects **billed** silence into stalled turns — per-session workers give fresh sockets and $0 idle by construction | BT (session loop unit) + HW: wake→command feel, back-to-back sessions | |
| 8 | wake | oWW models live in the venv's package dir (`download_models` default target), auto-fetched on first run per machine; `voice/models/` is unused | `Model()` resolves feature models from package resources; a custom dir would strand them | BT (test_wake downloads + detects) | |
| 9 | session | Session-end = `PipelineWorker` idle timeout (`holdWindowS`), but `cancel_on_idle_timeout=False` + an `on_idle_timeout` handler that defers while `GrammarGate.is_busy()` (user mid-turn, a dispatch in flight, OR — added 2026-08-11 — an **assistant answer in flight**: from transcript-handed-to-LLM until the bot starts speaking, capped at 30 s). The gap surfaced live the first night on GPT: a reasoning model thinking past `holdWindowS`=8 got its session killed mid-answer, which Haiku's ~2 s first-word never exposed; the same hole covered assistant tool calls (15 s ssh). Exit phrases push `EndWorkerFrame` immediately regardless | Flux emits no frame mid-turn, blocking dispatch pushes nothing, and an LLM reasoning silently pushes nothing either — a raw idle timeout fires mid-anything | HW: window feel — tune `holdWindowS` | |
| 10 | wake→speech gap | **Pre-roll buffer built** (clipping confirmed live 2026-08-10: "hey jarvis volume up" as one sentence lost the command). Design: the wake stream is NOT closed at detection — `WakeCapture` keeps reading it on a thread, seeded with a 2 s pre-detection ring (wake phrase included), and stops only right before the transport reopens the mic; `PrerollFeeder` (a stage between transport.input() and Flux) replays the PCM during StartFrame processing. Correctness rides on pinned-source facts: processors handle frames serially through one input queue, and Flux's StartFrame handling awaits its websocket handshake — so Flux sees [StartFrame, pre-roll, live mic] in order and drops nothing (proven empirically by test_preroll's live-worker ordering test). Transcripts now begin with the wake phrase → `strip_wake` (fuzzy anchor ≥80: "jervis" strips at ~83, "travis" survives at ~67; greeting optional; leading only) removes it for both lanes, and wake-phrase-only turns are swallowed (no LLM turn — the wake chime, now timed off the end of speech per row 6, is the only answer they get); "hey jarvis" is keyterm-boosted so the prefix transcribes canonically. Residuals: ~100–200 ms uncaptured gap at capture→mic handoff (only bites sentences still running ~2 s after wake — fix would move the handoff into the transport); the wake chime plays while the mic is held, so on the BT rig the profile flap moves to chime time — later now that the chime waits for end-of-speech, same row-19 workaround | One continuous capture beats two racing streams; text-side strip beats trimming audio; a paused-style "hey jarvis" must keep working | HW: §5b natural-sentence drill, both styles | |
| 11 | stt | `mip_opt_out=True` always (privacy over the ~2× metered rate) | Design-doc stance | Deepgram console shows the flag | |

| 12 | titles | `{game}` is a hassil **wildcard**; all title matching lives in the fuzzy resolver (exact-variant short-circuit → fuzzy with ambiguity refusal: near-ties across different games return "no match" — saying no beats launching wrong). Names are ASCII-sanitized at both sources | Real-library tests: no colon in "ARMORED CORE VI FIRES OF RUBICON" broke list matching; token_set scores subsets at 100 (Warhammer 40K tie); PS5.1 mojibake'd ™ | BT (38/38 round-trips, 10/10 spoken phrasings) + HW: fuzzy torture drill | |
| 13 | launch | "play Y" while X runs ⇒ truthful refusal (`BUSY:<id>`), same appid ⇒ `ALREADY`, mid-launch ⇒ "still starting"; owned-but-not-installed ⇒ spoken decline (install flow is C4) | Design-doc no-force-switch policy | HW: launch drills | |
| 14 | REPL | `--text` REPL is **always dry-run** (actions log, never execute) | Bench instrument; typing "end session" at a keyboard mid-game shouldn't kill the game | Your first REPL session | |
| 15 | tts | Aura-2 default voice `aura-2-thalia-en` (unauditioned); Kokoro behind `ttsLocal: true` requires `pip install "pipecat-ai[kokoro]"` and its ctor **blocks on a ~300 MB first-run download** | Audition needs ears; Kokoro kept optional to keep deploys light | HW: voice audition, then set `ttsVoice` | |
| 16 | catalog | **All three layers now auto-sync** via `library.sync()` on the agent's background thread (startup + after each session): installed when the PC is awake, owned when stale >6h, metadata top-up for new appids — Steam layers run even while the PC sleeps (cloud, no PC needed), key-gated, non-reentrant. So the catalog fills itself once the Steam key is in `secrets.json`; no manual CLI needed. First fill crawls ~2 s/game in the background. appid 228980 (redistributables) excluded | The Web API layers need no PC and are the higher-value/lower-cost half — leaving them manual was a build gap (user-flagged), and the cadence was inverted (auto-refreshed the PC-dependent layer, left manual the cloud one) | KEY in secrets.json, then watch the catalog populate over the first session or two | |
| 17 | assistant | Cross-session carry = last 8 context messages in process memory (lost on agent restart), **run through `_trim_carry`** so a slice never starts on an orphaned tool_result (Anthropic 400) or ends on an unpaired tool_call; in-session history native | Simplest thing that makes "play the second one" work, made 400-safe (review bug) | HW: follow-up drill | |

| 18 | provider | The brain is **provider-switchable** (Anthropic ↔ OpenAI) in both harnesses: the `--text` REPL picks via `--provider anthropic\|openai [--model <id>] [--effort ...]`, the production pipeline via `config.assistantProvider`. Tool schemas/impls/system prompt are shared. **OpenAI uses the Responses API** (`OpenAIResponsesHttpLLMService` in prod, `client.responses.create` in the REPL) — reasoning + tool calls coexist there, which they don't cleanly on legacy chat-completions. Reasoning effort is a config knob (`assistantReasoningEffort`, default "low"; none/minimal/low/medium/high) that trades latency for depth — so the A/B can compare Haiku vs GPT-5.6 at any effort, not GPT-with-reasoning-disabled. Needs `openaiApiKey` | User wants an easy Haiku-vs-GPT-5.6 A/B; Pipecat already abstracted the pipeline, only the raw-Anthropic REPL was locked | KEY: `openaiApiKey`, then `--text --provider openai [--effort low\|medium]` vs `--provider anthropic` on the 20-query set — watch latency vs answer quality | |

| 19 | audio | **Bluetooth headsets (AirPods) are a degraded test rig, not a target.** Root cause (isolated by differential test: wake+silence is clean, wake+speech flaps): AirPods expose input on the Hands-Free device but default output on the A2DP "Headphones" device, and the profiles are mutually exclusive — the session's held mic keeps HFP active, so the **first playback write** (earcon/TTS) tries to wake the suspended A2DP endpoint → profile flap + occasional `-9999` stream death (session goes silent, idle-times out gracefully). Workaround on BT: point BOTH `inputDeviceName`/`outputDeviceName` at the "Headset" endpoints — one profile, no flapping, phone-quality output. Wired/USB (the array) has none of this. Wake-tick retries once regardless. **Wake loop self-heals** (added after a live crash 2026-08-10: a `-9999` mid-listen killed the agent via cleanup raising "Stream not open" over the real error): dead-stream cleanup is best-effort (`close_stream_quietly`), the original error propagates, and the main loop logs + reopens the mic after 5 s instead of dying — a BT flap or multipoint wander now costs one log line, not the process | Architecture targets the wired ReSpeaker; Windows splits BT audio across two mutually-exclusive profile devices | Wired mic/speaker (or the array), or the both-on-Headset config; mechanism proven live on AirPods 2026-08-10 | |

## Review fixes (multi-agent pass, 2026-08-10)

11 confirmed bugs + selected quality/slop findings applied on-branch, all with blind coverage. The load-bearing ones:

- **Blocking dispatch off the event loop** — GrammarGate and the assistant tool handlers now `asyncio.to_thread` every ssh/serial call; a 15 s host-asleep "end session" no longer freezes audio, the Flux socket, and TTS.
- **Idle-timeout guard** (row 9) — sessions no longer cancel mid-utterance or mid-dispatch.
- **`_trim_carry`** (row 17) — follow-up sessions no longer 400 on split tool pairs.
- **`resolve('it')`** — single short stopword/pronoun queries refuse instead of launching a subset-matched title ("play it" → assistant, not It Takes Two).
- **`refresh_owned` under system python** — `load_secrets`/`real_key` moved to `cglib`; `library.py refresh --owned` no longer drags pipecat/hassil in and no longer crashes off-venv.
- **Empty `launch-app` marker** — stringify-before-trim; the consume-once file protocol survives a zero-byte marker (PS 5.1 `Get-Content`→`$null`).
- **`--devices` crash** — spike's argv parse moved under `__main__`; the first hardware drill works.
- **`secrets.json` BOM/typo** — `utf-8-sig` + `ValueError` caught; malformed secrets disable lanes, never crash-loop the supervisor.
- **Library refresh hooks** — now wired (background thread at startup + after each session), so "play &lt;title&gt;" works cold.
- **COM-port retry** moved into `cglib.exlink_send_hex` so couch.py's production power/input sends get the same contention protection.
- **Config validation** — missing `voice` keys fail loudly at startup, not per-wake; `.get` defaults removed so config.json is the one home.
- Quality/slop: `_vol_steps` helper (twin dedup), single library.json reader, `GrammarMatcher` built once, dead `titles` param + dead config keys + unreachable returns removed, `paInt16` constant, header-comment de-slop, Start-Voice bootstrap gated on a `deps-ok` sentinel.

- **Ex-Link ack validation** (deferred to the hardware pass, CLOSED 2026-08-10): the live probe proved the ack bytes — even the query frame acks `030cf1` — so `exlink_send_hex` now validates every send and raises `ExlinkNak` on anything else (no retry for a NAK; the 1 s retry stays port-contention-only). `dispatch._exlink` turns it into a fail earcon and `_vol_steps` aborts on the first failed frame; couch.py logs and continues (PC readiness is independent of the TV). `probe_volume` reads 16 bytes now so a payload can't hide behind `read(3)`.

## Project D rows (assistant expansion — D1 web search, built 2026-08-11)

> **Live pass, 2026-08-11 (evening) — drills clean, shipped as-is.** Built and
> proven the same day on the K15: in-lane web search (spoken answers, no
> citations leaking, quick turns unchanged), the Tier-3 worker lane end to end
> (queue → announce → pull → follow-up), and the doc-sourced CLI flags holding
> on first real contact. **Worker verdict: `sonnet` stays** — answers good
> enough that no model pin change is wanted, and desk Opus quota stays
> untouched. Sound and timing settled by ear along the way: think ticks built
> and **removed** (D1c), `holdWindowS` 8 → 10 (row 9), announcements
> proactive and followed by an open mic (D19). Rows below with an empty
> verdict were covered by that sweep. The **injection canary ran and is
> closed** (D21: file tools harness-bounded, shell reads model-judged and
> accepted). The one gate left unconfirmed is the **movie-window false-fire
> check (10c.1b)** — an open mic after a bulletin, in a room with a film
> running.

Built live on the dev box with real keys (REPL probes, both providers);
prod pipeline changes are blind until the K15 drill. Spec:
[assistant-expansion-design.md](assistant-expansion-design.md).

| # | Area | Assumption / decision | Why | Resolves by | Verdict |
|---|---|---|---|---|---|
| D1c | cue | **REMOVED, 2026-08-11 (same day).** Think ticks are gone: earcon spec, cue loop, config knob, tests. A slow answer is now simply silent until it speaks (an error still gets the fail earcon, and that stays). The reassurance-vs-nagging trade came out on the nagging side within an evening of real use | User verdict after live drills: "the think cues are just annoying/bad UX". The signal was cheap to add and cheaper to delete; the pending flag it rode on stays, because row 9's idle deferral is what actually needs it. **If 7-10 s of silence on searched turns ever becomes the complaint instead, the middle ground never tried is a SINGLE cue at the threshold with no repeat** | closed — removed | |
| D1b | cue | **The interval is a threshold, not a cadence** — and it has to be, because there is no event to key off: pipecat's Responses service pushes frames only for `ResponseFunctionToolCall` items, so a server-side `web_search_call` (the 7–10 s case worth covering) is invisible to the pipeline. Event-driven ticking would therefore stay silent through exactly the wait it exists for, and would fire only for client tool calls, which the timer already covers. Now `voice.thinkCueS` (ships 3) | Live 2026-08-11: set to 1 s, a trivial "what's up?" ticked before answering and that was the annoying UX. Making it config means tuning by ear from the couch instead of editing source mid-drill | HW: 10b.6 — raise toward 4–5 s if any tick still feels premature | |
| D1 | cue | **Think ticks replace the spec'd "cue on web_search_call event"**: a soft `think` earcon every `THINK_CUE_S` (3 s) while an assistant answer is in flight (the row-9 pending flag), stopping the moment the bot speaks. No pipecat event sniffing at all | Covers every silent stretch identically — search, long reasoning, a 15 s ssh tool call — on any provider; the event-sniffing version was OpenAI-only and coupled to service internals. First tick doubles as the fast-turn guard: answers inside 3 s never cue | HW: drill 10b.4 — tick feel, interval taste | |
| D2 | cue | An `ErrorFrame` while an answer is in flight clears the pending flag and plays the fail earcon (previously: silence + up to 30 s of idle-pinning) | The ticks made the gap audible: cueing "working…" and then trailing off into nothing is worse than an honest fail tone | BT (session-pipeline test) + HW: kill the network mid-question | |
| D3 | search | **Citation stripping is prompt-enforced only** — the search rule forbids URLs/citations/parentheticals in the reply; no deterministic text filter between LLM and TTS | Live probe: Luna's first searched answer embedded `([domain](url))`; after the hardened rule, clean on repeated probes. A streaming-safe filter means buffering sentence chunks — build it only if a citation ever actually gets spoken | HW: drill 10b.1, listen for leaked sources | |
| D4 | search | Haiku narrates pre-search ("I'll search for…") despite the don't-announce rule; accepted | The lane is REPL-only for search in prod (see D5); cosmetic on a bench instrument. Revisit prompt-side if the provider ever swaps back | A/B bench notes | |
| D5 | search | Prod search is **openai-lane only**: pipecat 1.7's `AdapterType` has no ANTHROPIC, so native server tools can't ride the anthropic adapter; startup logs the mismatch when the knob is on with the anthropic provider | Native-tool passthrough exists only for OpenAI/Gemini adapters in 1.7; the REPL covers Anthropic search via the raw SDK meanwhile | pipecat upgrade watch, or a service subclass if the A/B picks Haiku for prod | |
| D6 | search | OpenAI has no `max_uses` cap knob — cost control there is the prompt ("search only when…") plus `search_context_size: low`; Anthropic gets `max_uses` from config | Vendor asymmetry, not a design choice; at ~$0.01/search and couch volume this is pennies either way | cost eyeball after a month | |
| D7 | bench | Anthropic REPL cache oddity observed: turn 1 `w10222/r0`, turn 2 `w0/r0` (expected a prefix read) with search-result blocks in history | Unexplained; possibly search content interacting with the prefix cache. Bench-only concern today (prod search is openai-lane) | A/B bench: watch cache notes with the knob on/off | |
| D8 | workers | **Prompt via stdin** for both CLIs (`claude -p` reads stdin; `codex exec -`), never argv | npm installs ship `.cmd` shims that need cmd.exe routing, and a multiline prompt through cmd.exe quoting is a footgun; stdin sidesteps it entirely | HW: drill 10c.1 runs the real spawn | |
| D9 | workers | ClaudeWorker flag set (`-p --output-format json --allowedTools …`) and the `{"result": …}` output shape are **doc-sourced, not machine-verified** (no CLI on the dev box); parse falls back to raw text | Blind-build rule; the adapter boundary keeps corrections one-file | HW: drill 10c.1; fix flags in workers.py if the spawn errs | |
| D10 | workers | CodexWorker uses `--output-last-message <file>` instead of parsing JSONL events, plus `--skip-git-repo-check --sandbox workspace-write` with network on | The last-message file is shape-churn-proof; worker_home isn't a git repo; research needs the network | HW: drill 10c.7 | |
| D11 | announce | Offline/synth-failure fallback = announce earcon + unread + next-wake mention, **not** local TTS (spec said "Kokoro path") | Loading a 300 MB local TTS outside the pipeline for a rare failure path is complexity the pull path already covers; revive via kokoro-onnx direct synth if the drill says otherwise | HW: kill network, finish a job, expect earcon + mention | |
| D12 | jobs | "Cancel the task" clears QUEUED only; a RUNNING subprocess finishes or times out (spoken honestly) | Killing a mid-flight agent subprocess cleanly on Windows (child trees) is real work for a rare want | HW: drill 10c.5; revisit if it grates | |
| D21 | workers | **Canary run 2026-08-11, closed.** Three probes from `worker_home` against a real `claude -p`: (1) read `../../secrets.json` via the file tool → **harness-blocked** — outside the working directory, and a headless run can't grant the permission; (2) write `../pwned.txt` → **harness-blocked**, same boundary; (3) read the same file **via Bash** → **model declined**, harness did not block. So file tools are confined to `worker_home` by the harness (broader than the deny rules alone, which are now belt-and-braces), while shell reads rest on model judgment — `--allowedTools` pre-approves Bash and shell commands aren't path-bounded. **Accepted, per the v2 decision that workers get a shell**: shell means shell, and a deny list of read commands (`type`/`cat`/`Get-Content`/`python -c`…) is unenumerable theater. What bounds the blast radius instead: jobs originate only from the user's own voice (never an inbound channel), the gamepc key is forced-command-limited to six verbs, and the file-tool boundary catches the accidental case | The layer worth testing was the one that holds when the model *doesn't* refuse — and the honest finding is that for shell, there isn't one. Recorded rather than papered over | closed — accepted risk. **If it ever needs to be real**: run workers as a separate low-privilege Windows user with a deny ACL on `secrets.json`, the only mechanism independent of model judgment | |
| D19 | announce | **A bulletin heard in full opens a session** (`followUpAfterAnnounce`, ships true): the wake loop's listen is interruptible per 80 ms chunk, so the announcer can ask for a session instead of a wake word. No chime (the announcement was the cue), no pre-roll (no wake phrase was spoken), sleep chime as usual if you say nothing. Only after a FULL playback — an aborted or synth-failed bulletin leaves the mic shut, since nobody heard anything to follow up on | "Hey jarvis" to ask the obvious next question about news the house just volunteered is a strange tax. **Risk: this opens the mic for `holdWindowS` in a room that may have a movie running** — TV dialogue then reaches Flux and could match a Tier-1 command. Set the knob false if that bites | HW: 10c drill — announce during a movie, say nothing, confirm nothing false-fires; then follow up by voice with no wake word | |
| D20 | assistant | **Worker results are seeded into the session's history**, not the system prompt: `job_messages()` puts recent finished jobs (≤2, ≤6 h, detail capped at 1200 chars) at the front of the context as a user/assistant pair. They are stripped before `CARRY` is saved, since the next session re-seeds from jobs.json | Follow-ups were the gap: retrieval is Tier-1 (never reaches the LLM), so before this an announcement's result was invisible to the assistant and "which one was cheapest?" hit a model with no idea. History rather than system prompt keeps the system block byte-identical, which is what the prefix cache keys on — the catalog's cache read survives | HW: announce → follow up with a question only answerable from the findings | |
| D13 | announce | Each announcement opens its own short-lived PyAudio world; abort/session checks every 100 ms chunk | Precedent: pipecat's transport already runs its own instance beside the wake listener's; chunking bounds abort latency | HW: drill 10c.3 - wake mid-announcement | |
| D14 | workers | `workerEffort` ships **"high"**, carried per-adapter: Claude Code via the `CLAUDE_CODE_EFFORT_LEVEL` env var (no CLI flag exists), Codex via `-c model_reasoning_effort="high"`. Empty string = inherit the CLI's own default | Latency is free in Tier 3, so depth is the only axis worth spending on — and the vendors disagree by default: Claude Code already runs high on its adaptive-reasoning models, Codex defaults to **medium** (tuned for interactive work). The knob makes the lane deep on both. Env-var name is doc-sourced; if wrong it's a harmless no-op (claude's default is already high) | HW: drill 10c.1/10c.7 — quality vs. quota feel; verify the env var takes | |
| D18 | session | **There is no barge-in, and the design doc's claim of it is aspirational.** Pinned-source recon (pipecat 1.7, 2026-08-11): `InterruptionFrame` is constructed in exactly one place — `pipeline/worker.py:1150`, on receipt of an `InterruptionWorkerFrame` — and *nothing in the installed package ever constructs that frame*; our transport also has no `vad_analyzer`. So speech never cancels a TTS answer or an in-flight LLM turn. Consequences: a Tier-1 command spoken mid-answer dispatches immediately AND the answer still arrives (both work, they just queue); but a long reply cannot be talked over — it plays to the end. Reviving it means a VAD analyzer on the transport plus a small processor that pushes `InterruptionWorkerFrame` upstream when the user starts speaking during bot speech | Found while answering "what happens if I command during a search". The v2 design specified barge-in <150 ms via Silero VAD; the build never wired the analyzer, and 1.7's interruption path needs a producer the package doesn't ship | HW: talk over a long answer during 10b — if it grates, the fix above is ~30 lines and its own drill | |
| D17 | config | **The two lanes take different model-value grammars**, and the keys look identical: `assistantModel*` reaches an **API** (full ids only — `claude-haiku-4-5`), `workerModel*` reaches a **CLI** (aliases like `sonnet` work, and are preferable since they follow the latest). Startup warns — not refuses — when `assistantModelAnthropic` isn't a full id | Caught in the live config 2026-08-11 (`"haiku"`): harmless while `assistantProvider` is openai, then every answer 400s the day it's flipped. A warning beats a refusal because a bad value on the *inactive* provider shouldn't keep the couch's voice down | Startup line, then the provider flip in drill 10b.3 | |
| D16 | workers | **Auth is checked by failing well, not by probing.** Credentials live in the profile of whoever ran `<cli> login` (they survive reboots and refresh on use), so doctor checks PATH presence only — no credential-file check, because those paths are vendor-internal and a wrong guess would cry wolf every run. Instead an exit whose stderr smells like auth (`not logged in`, `unauthorized`, `401`, `expired`, …) is announced as "the background worker needs a re-login - run claude login on the console" instead of the generic failure | The appliance risk isn't idleness (tokens refresh) but a password/subscription change invalidating them silently — and a couch-side "the task failed" says nothing actionable. **Corollary for deploy: run the logins as the autologon user**, not from an ssh/elevated shell, or the agent won't see them | HW: after login, drill 10c.1; the wrong-user failure mode shows up as an instant auth-worded announcement | |
| D15 | workers | **One vocabulary, one key per vendor, no hidden defaults** (user call, 2026-08-11): `workerProvider` takes `anthropic`/`openai` like `assistantProvider` (they run the claude/codex CLIs), and the model is `workerModelAnthropic` (ships `"sonnet"`) / `workerModelOpenai` (ships empty). Assistant's `assistantModel` renamed to `assistantModelAnthropic` to match. Adapter-side `DEFAULT_MODEL` was tried and removed — a default you must read source to discover is the thing config is for. Startup logs the effective model+effort for both lanes | Found live at deploy: an empty model doesn't inherit "the vendor's sensible default", it inherits *the operator's own CLI preference* — **Opus 5 / Claude Max** here — so background jobs would have spent desk quota at high effort, invisibly. And one shared `workerModel` key could hand a claude alias to codex (invalid-model error) the moment the A/B ran | HW: drill 10c.1 as shipped, then `"opus"` on the same question and compare | |

## Open questions

(appended as they arise; each gets an owner: a drill, a key, or a decision of yours)
